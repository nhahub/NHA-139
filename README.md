🌐 Where 2 Go: Local Business & Entertainment Directory PlatformA comprehensive, geo-aware platform designed to help users discover and plan outings based on their defined budget and location across Egyptian governorates.🚀 FeaturesThe "Where 2 Go" application is built around advanced search, robust authentication, and personalized user experiences.Core Discovery & FilteringBudget-Based Search: Advanced filtering by Price Level (1-4) to find affordable destinations.Geographical Sorting: Radius Sort functionality to display places based on proximity to the user's current location (lat/lng).Advanced Filters: Filter listings by City and Category.User Personalization & ManagementFavorites System: Authenticated users can save and manage favorite locations.History Tracking: Logs and displays recently viewed places.Role-Based Access Control (RBAC): Distinct roles for Customer, Owner, and Admin.Owner & Admin WorkflowListing Submission: Owners can submit new listings which automatically enter a "pending" status.Admin Approval Lifecycle: Admins have full control to approve, reject, or delete listings.Owner Management: Owners can update their listings, which triggers a mandatory re-approval (re-pending).💻 Tech StackThe system utilizes a modern, modular architecture across three layers:FrontendComponentTechnologiesFrameworkReact + TypeScriptStylingTailwind CSS + ShadCN/UI (for robust components)State/DataZustand, React Query (TanStack Query)Globalizationreact-i18next (for Ar/English and RTL Support)MappingLeaflet (react-leaflet)BackendComponentTechnologiesServerNode.js + Express.jsDatabaseMongoDB (NoSQL)AuthenticationJWT (JSON Web Tokens) + bcryptjs (for secure password hashing)Data Sourcesapify-client (used for initial place data scraping)👥 Team & ResponsibilitiesThe project development was executed following a structured workflow, with clear responsibilities assigned to each team member (Roles are updated):MemberRole FocusKey Tasks DeliveredAmira HeshamGlobalization & DashboardImplemented Ar/English Language Support, Oversaw Dashboard UI, Prepared Documentation.Saif HarrazListing ManagementBuilt Add Listing and Listing Details interfaces.Amr AhmedCore Pages & UIDeveloped the Home page and the main Listing catalog page.Mohamed HossamSystem Logic & ContactDeveloped core Backend API logic and the Contact interface.Awad FahimAuthentication SystemBuilt the Backend for Sign Up/Sign In and user access management.🛠️ Installation & SetupTo run the project locally, follow these steps. (Assumes Node.js and npm/yarn are installed.)Bash# 1. Clone the repository
git clone <YOUR_REPOSITORY_URL>
cd where-2-go

# 2. Install dependencies for both front and back (if Monorepo setup, otherwise install in respective folders)
npm install
# or
yarn install

# 3. Configure Environment Variables
# Create a .env file in the root directory (or backend folder) with your MongoDB URI and JWT secrets.
MONGO_URI=<YOUR_MONGODB_CONNECTION_STRING>
JWT_SECRET=<YOUR_SECRET_KEY>

# 4. Start the development server
npm run dev-backend  # or similar command for your backend
npm run dev-frontend # or similar command for your frontend
The application will be accessible at http://localhost:3000 (Frontend) and the API endpoints at http://localhost:5000/api/v1/ (Backend, assuming port 5000).🔑 API Endpoints OverviewThe backend uses a RESTful structure with strict Role-Based Access Control (RBAC) enforced via JWT tokens.FunctionalityMethodEndpointAccessAdvanced SearchGET/api/v1/places/searchPublicSign Up/LoginPOST/api/auth/signup / /loginPublicSubmit New ListingPOST/api/listings/submitOwner OnlyApprove/Reject ListingPATCH/api/listings/:id/statusAdmin OnlyAdd to FavoritesPOST/api/users/favoritesAuthenticated🗺️ System ArchitectureThe application adopts a decoupled architecture for resilience and scalability.**Getty ImagesExplore**Frontend (Presentation Layer): Handles the UI and user interaction, making API calls.Backend (Application Logic Layer): Manages all business rules, authentication, and data transactions.Database (Data Layer): Persistent data storage in MongoDB.
